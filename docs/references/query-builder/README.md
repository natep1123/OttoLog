# Query builder — reference shots

Canonical after-gallery for Insights QB (madlib → WITH/SPLIT B+C → **§11 nest
chrome** author + lock readout).

Workout nest DNA (incl. override dusk for Insight chrome): [`../workout-builder/`](../workout-builder/) — esp. `08` / `09`.  
Archives: [`../archive/query-pre-madlib/`](../archive/query-pre-madlib/),
[`../archive/query-madlib-2.5-early/`](../archive/query-madlib-2.5-early/).

## Gallery

| File | What it shows |
|------|----------------|
| `01-query-locked-expanded.jpg` | Lock ON — scope meta → purple For each → Gait groups + totals |
| `02-locked-preview-p1.jpg` | Maximize → `LockedPreviewModal` page 1 *(older; re-shoot optional)* |
| `06-madlib-author-show-split.jpg` | Pre–§11 flat clause — SHOW chips, WITH, SPLIT *(historical author)* |
| `07-madlib-author-measure-rail.jpg` | Pre–§11 measure rail under FOR *(historical author)* |
| `08-author-where-for-insight.jpg` | **§11 author** — blue WHERE → gold FOR Gait → dusk **Insight 1** (WITH/SHOW) + add cascade |

## Still empty / optional later

| File | Capture |
|------|---------|
| `05-madlib-author-empty.jpg` | Progressive empty (FOR only) |
| `03-locked-preview-p2.jpg` | Preview page 2 if outline paginates |
| `09-more-query.jpg` | Query More (name/notes) |
| `10-author-insight-filled.jpg` | §11 Insight with measures + Match / SPLIT seed |

## Product notes (Jul 23)

- **§11 nest chrome in progress** — WHERE/FOR/Insight rails (`08`). Insight borrows
  workout dusk chrome (`workout-builder/08`–`09`), not the word “override.”
- **WITH/SPLIT B+C shipped** — any/all + sibling SPLIT (`01` / `06`).
- **Follow-on:** SHOW halves → dropdowns; chip truncation; `availableFields`.

## Kickoff line

> Read `docs/references/query-builder/README.md`, then open every `.jpg` in that folder with the Read tool. Also open `docs/references/workout-builder/` (esp. `08`/`09` for Insight dusk DNA).
