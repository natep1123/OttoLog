# Query builder — before / after open (pool)

Working shots for Insights QB open state. Canonical **after** lean gallery will
live in [`../query-builder/`](../query-builder/) once dogfood / slice 2 fills it
(checklist only today — don’t invent files).

Gold **structure/feel** target remains [`../workout-builder/`](../workout-builder/)
(Murph Session). Content inside QB nodes stays analytics.

| File | What |
|------|------|
| `before-open-cool-palette.jpg` | Slice 1 open — provisional cool teal/aqua (before Decision 12 / 1.5) |
| `after-open-warm-rails.jpg` | Slice 1.5 open — warm rails by depth, lock toggles, Query More (still dense; grammar/preview = slice 2) |

## Kickoff line

> For QB chrome work, open both `.jpg` files in this folder with the Read tool,
> plus every `.jpg` under `docs/references/workout-builder/`.
