# Workout builder — reference shots

**Source:** Library → Templates → Sessions → **Murph — Full Session**

Gold DNA for nested form chrome. QB reuses this structure/feel — **same
`layer`/`override` accents by depth** (Decision 12); content inside QB nodes
stays analytics.

## Gallery (keepers)

| File | What it shows |
|------|----------------|
| `01-session-locked-expanded.jpg` | Session lock ON, expanded — full locked outline |
| `02-locked-preview-p1.jpg` | `LockedPreviewModal` page 1 of 3. *Minor known issue: last content line clips under the page footer — acknowledge, ignore for now.* |
| `03-locked-preview-p2.jpg` | Preview page 2 (continuation + long set list) |
| `04-session-blocks-mixed.jpg` | Session locked; Warmup locked+expanded; other Blocks collapsed with pills |
| `05-exercise-unlocked-form.jpg` | Editable exercise leaf (Tools / Shape / sets) + collapsed siblings |
| `06-more-session.jpg` | Session More open (Name/Brief, notes) — Query-More analog |
| `07-add-cascade-footer.jpg` | `+ Add … → parent` cascade + Save / Done chrome |

## Kickoff line

> Read `docs/references/workout-builder/README.md`, then open every `.jpg` in that folder with the Read tool before changing form chrome.

Raw dump + optionals: [`../pool-template-log-builder/`](../pool-template-log-builder/).
