# Workout builder — reference shots

**Source:** Library → Templates → Sessions → **Murph — Full Session** (and Sequence
override chrome as needed)

Gold DNA for nested form chrome. QB reuses this **structure/feel** with analytics
content. For §11 Insight dusk cards: open **`08` / `09`** — workout **override**
chrome (dusk pink under Sequence/Exercise). Insight borrows that *chrome*, not
the product word “override.”

## Gallery

| File | What it shows |
|------|----------------|
| `01-session-locked-expanded.jpg` | Session lock ON, expanded — full locked outline |
| `02-locked-preview-p1.jpg` | `LockedPreviewModal` page 1. *Minor known issue: last line can clip under footer — ignore for now.* |
| `03-locked-preview-p2.jpg` | Preview page 2 |
| `04-session-blocks-mixed.jpg` | Mixed expand / collapsed Blocks |
| `05-exercise-unlocked-form.jpg` | Editable exercise leaf (in Block) |
| `06-more-session.jpg` | Session More (Name/Brief, notes) |
| `07-add-cascade-footer.jpg` | `+ Add … → parent` + Save/Done |
| `08-sequence-exercise-with-overrides.jpg` | Exercise inside Sequence — tools/shape/sets + dusk **Overrides** list (`+ Add override → Sequence`) |
| `09-override-editor.jpg` | Override editor open — rounds, skip, change toggles, notes, `Add override` |

Optionals (older extras): [`../archive/workout-optionals/`](../archive/workout-optionals/).

## Kickoff line

> Read `docs/references/workout-builder/README.md`, then open every `.jpg` in that folder with the Read tool.
