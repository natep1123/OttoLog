# Deprecated incremental SQL (`001`–`019`)

Historical migrations applied to the original development Supabase project.

**Do not** run these on a greenfield database. Prefer `sql/greenfield/001`–`007` for new projects (see `docs/Setup.md`).

Kept for reference and for understanding what the live (pre-greenfield) schema looked like.
